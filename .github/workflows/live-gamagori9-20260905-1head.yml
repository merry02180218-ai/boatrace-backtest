name: live-gamagori9-20260905-1head
on:
  push:
    branches: [ main ]
    paths:
      - 'live_gamagori9_20260905_1head.py'
      - '.github/workflows/live-gamagori9-20260905-1head.yml'
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install numpy pandas scikit-learn requests beautifulsoup4
      - run: python live_gamagori9_20260905_1head.py
      - name: Commit result
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "418982818+github-actions[bot]@users.noreply.github.com"
          git add live_gamagori9_20260905_1head.md
          if ! git diff --cached --quiet; then
            git commit -m "Generate Sep 5 Gamagori9 1-head live result"
            git pull --rebase origin main
            git push
          fi
