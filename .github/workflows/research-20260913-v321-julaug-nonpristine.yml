name: v321 Jul-Aug NON-PRISTINE validation

on:
  push:
    paths:
      - 'run_v321_1head_julaug_nonpristine_validation.py'
      - '.github/workflows/research-20260913-v321-julaug-nonpristine.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  prepare:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install numpy pandas scipy 'scikit-learn==1.6.1' requests beautifulsoup4 lxml
      - name: Require development inputs
        run: |
          test -f analysis_v308_1head_volume_opponent_joint_pred.csv
          test -f CACHE_V313_1HEAD_OPPONENT_CAUSAL.md
      - name: Prepare Jul-Aug causal NON-PRISTINE cache
        run: python run_v321_1head_julaug_nonpristine_validation.py --stage prepare
      - uses: actions/upload-artifact@v4
        with:
          name: v321-julaug-prepare
          path: |
            cache_v321_julaug_nonpristine_slim.csv.gz
            cache_v321_julaug_nonpristine_head.csv
            cache_v321_julaug_nonpristine_meta.csv

  second:
    needs: prepare
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install numpy pandas scipy 'scikit-learn==1.6.1' requests beautifulsoup4 lxml
      - uses: actions/download-artifact@v4
        with:
          name: v321-julaug-prepare
          path: .
      - name: Build frozen SECOND predictions
        run: python run_v321_1head_julaug_nonpristine_validation.py --stage second
      - uses: actions/upload-artifact@v4
        with:
          name: v321-julaug-second
          path: cache_v321_julaug_second.pkl

  base-third:
    needs: prepare
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install numpy pandas scipy 'scikit-learn==1.6.1' requests beautifulsoup4 lxml
      - uses: actions/download-artifact@v4
        with:
          name: v321-julaug-prepare
          path: .
      - name: Build frozen BASE THIRD predictions for opponent mass
        run: python run_v321_1head_julaug_nonpristine_validation.py --stage base-third
      - uses: actions/upload-artifact@v4
        with:
          name: v321-julaug-base-third
          path: cache_v321_julaug_base_pc.pkl

  third:
    needs: prepare
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install numpy pandas scipy 'scikit-learn==1.6.1' requests beautifulsoup4 lxml
      - uses: actions/download-artifact@v4
        with:
          name: v321-julaug-prepare
          path: .
      - name: Build frozen v318 THIRD predictions
        run: python run_v321_1head_julaug_nonpristine_validation.py --stage third
      - uses: actions/upload-artifact@v4
        with:
          name: v321-julaug-third
          path: cache_v321_julaug_third.pkl

  score:
    needs: [prepare, second, base-third, third]
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install numpy pandas scipy 'scikit-learn==1.6.1' requests beautifulsoup4 lxml
      - uses: actions/download-artifact@v4
        with:
          name: v321-julaug-prepare
          path: .
      - uses: actions/download-artifact@v4
        with:
          name: v321-julaug-second
          path: .
      - uses: actions/download-artifact@v4
        with:
          name: v321-julaug-base-third
          path: .
      - uses: actions/download-artifact@v4
        with:
          name: v321-julaug-third
          path: .
      - name: Score frozen ticket policy
        run: python run_v321_1head_julaug_nonpristine_validation.py --stage score
      - uses: actions/upload-artifact@v4
        with:
          name: v321-julaug-nonpristine
          path: |
            analysis_v321_1head_julaug_nonpristine_validation_*.csv
            summary_v321_1head_julaug_nonpristine_validation.md
      - name: Commit v321 outputs
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add analysis_v321_1head_julaug_nonpristine_validation_*.csv summary_v321_1head_julaug_nonpristine_validation.md
          if ! git diff --cached --quiet; then
            git commit -m 'Add v321 Jul-Aug NON-PRISTINE validation results'
            git push
          fi
