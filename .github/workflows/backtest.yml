name: monthly-backtest
on:
  workflow_dispatch:
  push:
    branches: [ main ]
jobs:
  backtest:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run v12 ROI-first 3-boat model
        run: python backtest_v12_3roi.py
      - name: Commit results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "418982818+github-actions[bot]@users.noreply.github.com"
          git add summary_v12_3roi.md
          [ -f bets_v12_3roi.csv ] && git add bets_v12_3roi.csv || true
          [ -f races_v12_3roi.csv ] && git add races_v12_3roi.csv || true
          if ! git diff --cached --quiet; then
            git commit -m "Run v12 ROI-first 3-boat backtest"
            git push
          fi
