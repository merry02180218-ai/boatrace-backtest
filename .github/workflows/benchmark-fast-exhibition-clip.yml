name: benchmark fast exhibition clip

on:
  push:
    paths:
      - '.github/workflows/benchmark-fast-exhibition-clip.yml'
      - 'download_boatcast_exhibition_fastclip.py'
      - 'auto_seed_exhibition_motion_v3.py'
      - 'track_exhibition_boats_v5.py'
      - 'sweep_exhibition_ses_anchor_v1.py'
      - 'sweep_boat1_seed_offset_v1.py'
  workflow_dispatch:
    inputs:
      race_date:
        description: YYYYMMDD
        required: true
        default: '20260910'
      stadium:
        description: e.g. 01kiryu
        required: true
        default: '01kiryu'
      race:
        description: race number 1-12
        required: true
        default: '12'
      clip_start_sec:
        description: VOD seek start seconds
        required: true
        default: '82'
      clip_duration_sec:
        description: clip duration seconds
        required: true
        default: '52'
      entry_order:
        description: top-to-bottom exhibition entry order
        required: true
        default: '1,2,3,4,5,6'

permissions:
  contents: read
  actions: read

jobs:
  fastclip:
    runs-on: [self-hosted, japan]
    timeout-minutes: 8
    env:
      RACE_DATE: ${{ github.event_name == 'workflow_dispatch' && inputs.race_date || '20260910' }}
      STADIUM: ${{ github.event_name == 'workflow_dispatch' && inputs.stadium || '01kiryu' }}
      RACE: ${{ github.event_name == 'workflow_dispatch' && inputs.race || '12' }}
      CLIP_START_SEC: ${{ github.event_name == 'workflow_dispatch' && inputs.clip_start_sec || '82' }}
      CLIP_DURATION_SEC: ${{ github.event_name == 'workflow_dispatch' && inputs.clip_duration_sec || '52' }}
      ENTRY_ORDER: ${{ github.event_name == 'workflow_dispatch' && inputs.entry_order || '1,2,3,4,5,6' }}
      LOCAL_PYTHON: C:\Users\merry\AppData\Local\Programs\Python\Python311\python.exe
    steps:
      - uses: actions/checkout@v4
      - name: Verify preinstalled runtime
        shell: powershell
        run: |
          & $env:LOCAL_PYTHON --version
          ffmpeg -version | Select-Object -First 1
          & $env:LOCAL_PYTHON -c "import playwright,cv2,numpy; print('runtime ok')"
      - name: Fetch only start-exhibition clip
        shell: powershell
        run: '& $env:LOCAL_PYTHON download_boatcast_exhibition_fastclip.py'
      - name: Auto-detect six hybrid hull seeds v3
        shell: powershell
        run: |
          $video = Get-ChildItem -Filter 'boatcast_*_startclip.mp4' | Select-Object -First 1
          if (-not $video) { throw 'startclip not found' }
          $sw = [Diagnostics.Stopwatch]::StartNew()
          & $env:LOCAL_PYTHON auto_seed_exhibition_motion_v3.py $video.FullName --entry-order $env:ENTRY_ORDER --nominal-sec 29 --out auto_seed_meta_v3.json --seed-out auto_seeds_v3.json
          if ($LASTEXITCODE -ne 0) { throw 'auto seeding v3 failed closed' }
          $sw.Stop()
          "AUTO_SEED_V3_SECONDS=$($sw.Elapsed.TotalSeconds)"
      - name: Sweep boat1 seed offsets result-blind
        shell: powershell
        run: |
          $video = Get-ChildItem -Filter 'boatcast_*_startclip.mp4' | Select-Object -First 1
          $meta = Get-Content auto_seed_meta_v3.json -Raw | ConvertFrom-Json
          $slit = [string]$meta.selected_sec
          $sw = [Diagnostics.Stopwatch]::StartNew()
          & $env:LOCAL_PYTHON sweep_boat1_seed_offset_v1.py $video.FullName --slit-sec $slit --seed-json auto_seeds_v3.json --x-offsets '-120,-80,-40,0,40,80,120' --y-offsets '-12,0,12' --out boat1_seed_sweep_v1.json
          if ($LASTEXITCODE -ne 0) { throw 'boat1 seed sweep failed' }
          $sw.Stop()
          "BOAT1_SEED_SWEEP_SECONDS=$($sw.Elapsed.TotalSeconds)"
          $s = Get-Content boat1_seed_sweep_v1.json -Raw | ConvertFrom-Json
          $s.best_tracking_quality_candidate | ConvertTo-Json -Depth 8
      - name: Track baseline and score SES v5
        shell: powershell
        run: |
          $video = Get-ChildItem -Filter 'boatcast_*_startclip.mp4' | Select-Object -First 1
          $meta = Get-Content auto_seed_meta_v3.json -Raw | ConvertFrom-Json
          $slit = [string]$meta.selected_sec
          $sw = [Diagnostics.Stopwatch]::StartNew()
          & $env:LOCAL_PYTHON track_exhibition_boats_v5.py $video.FullName --slit-sec $slit --seed-json auto_seeds_v3.json --out exhibition_tracking_v5.json --stride 2
          if ($LASTEXITCODE -ne 0) { throw 'SES tracking v5 failed closed' }
          $sw.Stop()
          "SES_TRACK_SECONDS=$($sw.Elapsed.TotalSeconds)"
      - name: Show latency and outputs
        shell: powershell
        run: |
          $m = Get-ChildItem -Filter 'boatcast_*_startclip.json' | Select-Object -First 1
          Get-Content $m.FullName
          Get-Content auto_seed_meta_v3.json
          Get-Content exhibition_tracking_v5.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: fastclip-boat1-seed-sweep-${{ env.RACE_DATE }}-${{ env.STADIUM }}-${{ env.RACE }}
          path: |
            boatcast_*_startclip.mp4
            boatcast_*_startclip.json
            auto_seed_meta_v3.json
            auto_seeds_v3.json
            boat1_seed_sweep_v1.json
            _b1_seed_*.json
            _b1_track_*.json
            exhibition_tracking_v5.json
          if-no-files-found: warn
